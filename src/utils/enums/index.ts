export enum LayoutMode {
  // 纵向
  vertical = 'vertical',
  // 经典
  classic = 'classic',
  // 横向
  transverse = 'transverse',
  // 分栏
  columns = 'columns',
}

export enum Size {
  // 默认
  default = 'default',
  // 大
  large = 'large',
  // 小
  small = 'small',
}

export enum Locale {
  // 中文
  zh = 'zh',
  // 英文
  en = 'en',
}
